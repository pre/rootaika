requests>=2.31
Pillow>=10.0
# Waveshare e-Paper driver. The pip package may lag behind hardware revisions,
# so prefer the vendor library if the panel does not refresh correctly. See
# README.md ("Waveshare-ajurin asennus") for the git clone alternative.
waveshare-epd>=1.0; platform_machine == "armv6l" or platform_machine == "armv7l" or platform_machine == "aarch64"
# Raspberry Pi GPIO/SPI backends used by the Waveshare driver.
RPi.GPIO>=0.7; platform_system == "Linux"
spidev>=3.5; platform_system == "Linux"
